spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test?serverTimezone=UTC&nullCatalogMeansCurrent=true
    username: root
    password: root

  mvc:
    servlet:
      path: /

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
#      naming:
#        implicit-strategy:
  thymeleaf:
    cache: false

#  thymeleaf:
#    prefix: classpath:/templates/
flowable:
  async-executor-activate: false
